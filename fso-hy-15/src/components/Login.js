import React from 'react'
import { LOGIN } from '../queries'

const Login = (props) => {
  if (!props.show) {
    return null
  }

  return (
    <div>
      <h2>login</h2>

      
    </div>
  )
}

export default Login